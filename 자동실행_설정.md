# AI Avatar 서비스 자동 실행 방법

## 방법 1: 바로가기 생성 (가장 간단)

### 수동으로 바로가기 만들기:
1. `run.bat` 파일을 우클릭
2. "바로가기 만들기" 선택
3. 생성된 바로가기를 바탕화면이나 작업 표시줄로 이동
4. 더블클릭하면 자동 실행!

### 바로가기 속성 설정:
- 우클릭 → 속성 → 고급
- "관리자 권한으로 실행" 체크 (필요시)

---

## 방법 2: Windows 시작 프로그램에 등록 (부팅 시 자동 실행)

### PowerShell로 등록:
```powershell
# 시작 프로그램 폴더에 바로가기 복사
$StartupPath = "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup"
$ShortcutPath = "$StartupPath\AI Avatar 서비스.lnk"
$WshShell = New-Object -ComObject WScript.Shell
$Shortcut = $WshShell.CreateShortcut($ShortcutPath)
$Shortcut.TargetPath = "C:\AIAvata\AIAvata\run.bat"
$Shortcut.WorkingDirectory = "C:\AIAvata\AIAvata"
$Shortcut.Save()
```

### 수동으로 등록:
1. `Win + R` 키 누르기
2. `shell:startup` 입력 후 Enter
3. `run.bat` 파일의 바로가기를 이 폴더에 복사
4. 컴퓨터 재시작 시 자동 실행됨

---

## 방법 3: 작업 스케줄러 설정 (고급)

### 작업 스케줄러로 등록:
1. 작업 스케줄러 열기 (`Win + R` → `taskschd.msc`)
2. "기본 작업 만들기" 클릭
3. 이름: "AI Avatar 서비스"
4. 트리거: "컴퓨터 시작 시" 또는 "사용자 로그온 시"
5. 작업: "프로그램 시작"
6. 프로그램: `C:\AIAvata\AIAvata\run.bat`
7. 시작 위치: `C:\AIAvata\AIAvata`

---

## 방법 4: 서비스로 등록 (백그라운드 실행)

### NSSM 사용 (Non-Sucking Service Manager):
1. NSSM 다운로드: https://nssm.cc/download
2. 설치 후 명령어 실행:
```cmd
nssm install AIAvatarService "C:\AIAvata\AIAvata\run.bat"
nssm set AIAvatarService AppDirectory "C:\AIAvata\AIAvata"
nssm start AIAvatarService
```

---

## 방법 5: 간단한 실행 스크립트 사용

프로젝트 루트에 `start_avatar.bat` 파일이 있습니다.
이 파일을 더블클릭하면 자동으로 서버가 시작됩니다.

---

## 추천 방법

**일반 사용자**: 방법 1 (바로가기 생성) - 가장 간단하고 안전
**자동 실행 원함**: 방법 2 (시작 프로그램 등록) - 부팅 시 자동 실행
**백그라운드 실행**: 방법 4 (서비스 등록) - 창 없이 실행

---

## 주의사항

- 서버를 종료하려면 실행된 창들을 모두 닫아야 합니다
- 포트 충돌이 발생하면 기존 서버를 먼저 종료하세요
- 방화벽에서 포트 8000, 5173을 허용해야 할 수 있습니다

